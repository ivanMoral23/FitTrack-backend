# PLANTILLA — NO commitear con valores reales.
# Crear el Secret real con el comando del README (kubectl create secret ...).
# Este archivo es solo documentación de la estructura. ArgoCD lo excluye.
apiVersion: v1
kind: Secret
metadata:
  name: gymtracker-secrets
  namespace: gymtracker
type: Opaque
stringData:
  SECRET_JWT_KEY: "REPLACE_ME"
  USDA_API_KEY: "REPLACE_ME"
  MONGO_URI: "mongodb://gymtracker-mongodb:27017/gymtracker"
